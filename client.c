#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>
#include <sys/socket.h>
#include "message.h"

int main(int argc, char *argv[]) {
    printf("Client started. Type commands (e.g., /login <client ID> <password> <server-IP> <server-port>)\n");
    
    // TODO: Initialize client socket when needed (login)
    // TODO: Implement command processing loop (stdin)
    
    return 0;
}
